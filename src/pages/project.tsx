import React from "react";
import { ContentPageContainer } from "../components/Layout/HomePageStyle";

const project = () => {
  return <ContentPageContainer maxWidth={1024}></ContentPageContainer>;
};

export default project;
